import logging


logging.basicConfig(filename="polymosrphism1.log", level=logging.INFO, format='%(levelname)s %(asctime)s %(name)s %(message)s')

class ineuron:
    def __init__(self,name):
        slef.name=name
    try:
        def vipin(self):
            logging.info(f"{self.name} is a student of vipin at ineuron ")
    except Exceptionas as e:
        logging.info(e)

class ineuron_job:
    def __init__(self,name):
        self.name=name

    try:
        def vipin(self):
            logging.info(f"{self.name} is a colleague of vipin at ineuron")
    except Exception as e:
        logging.info(e)
class internship:
    def __init__(self,name):
        self.name=name
    try:
        def vipin(self):
            logging.info(f"{self.name} is teammate of vipin in internship")
    except Exception as e:
        logging(e)
